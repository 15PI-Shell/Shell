1) cat: ереименован в prFile
	не прописывается директория это часть имени файла
	из 2-х аргументов берет последний ужен не берет, выдает ошибку
	падает на cat 1 Binary.h (выводит Binary.h и падает) падает при 2-х аргументах, если первый - число или несущестующий файл - не падает
    на пустом файле не выводит сообщение о том, что он пустой (не баг, просто ты говорила, что проверяется) выводится
   
2) binary
	из 2-х аргументов берет первый
	принимает строку и возвращает 0
	на отрицательное число возвращает '-' и двоичное представление ???
	стало падать без аргументов

3) diff
	не прописывается директория и не должна это часть имени файла представляешь сколько мороки обрабатывать 4 аргумента сверяя это директория или нет
	падает с двумя одинаковыми аргументами
	diff a a выводит сразу 2 ошибки: Arguments are wrong & files opening error. Это нормально??? уе не выводит
	глобально падает на 1 аргументе и без них не падает
	
4) history
	от вещественного числа берет только целую часть (может и норм:D)
	от двух аргументов берет первое
	История перезаписывается после ввода 100 команд, а не удаляется 1 строка

5) ls
	при несуществующем пути не выводит ошибку ??? это не моя вина, на сществование директорию проверяет уже только твой поиск
	для трех аргументов падает не падает
	
6) tail
	не прописывается директория и не должна, это часть имени файла
	не может считать строки из .asm (выводит только первую), хотя diff с ними справляется
	
7) ||, &&
	программа падает после открытия 1 файла
