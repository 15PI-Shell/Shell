1) cat: ереименован в prFile
	не прописывается директория это часть имени файла !!!
   
2) binary
	из 2-х аргументов берет второй
	принимает строку и возвращает 0
	на отрицательное число возвращает '-' и двоичное представление ???
	стало падать без аргументов

3) diff
	не прописывается директория и не должна это часть имени файла представляешь сколько мороки обрабатывать 4 аргумента сверяя это директория или нет
	
4) history
	от вещественного числа берет только целую часть (может и норм:D) ну да как бы где ты видела вещественной число строк?)

	История перезаписывается после ввода 100 команд, а не удаляется 1 строка  походу баг не мой т.к. после удаления возврашщается указатель на удаленный пустой узел. пока будем считать что так и задумано

5) ls
	при несуществующем пути не выводит ошибку ??? это не моя вина, на сществование директорию проверяет уже только твой поиск
	для ls *.com -r не выводит сообщение об ошибке
	
6) tail
	не прописывается директория и не должна, это часть имени файла
	не может считать строки из .asm (выводит только первую), хотя diff с ними справляется Вообще. у них с диффом чтение файлов устроено одинаково, да и у себя такого бага не заметила на компе
	
7) ||, &&
	программа падает после открытия 1 файла

8) cd ../.. меняет 